<h2>
  <%= @movie.title %>
</h2>

<ul>
  <% @tags.each do |tag|%>
    <li>
      <%= tag.name %>
    </li>
  <% end %>
</ul>

<ul>
  <% @comments.each do |comment|%>
    <li>
      <%= comment.body %>
    </li>
  <% end %>
</ul>
